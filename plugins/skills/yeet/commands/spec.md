---
description: Write a spec for a new feature. Conducts a brainstorm if the brief is fuzzy. Skips if the work does not warrant a spec.
argument-hint: optional brief description of the feature
---

Use the Agent tool with `subagent_type: "yeet-spec"` to handle this request. Pass the user's full request to the agent as its prompt.

User request: $ARGUMENTS
