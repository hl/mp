---
description: Document a completed feature to docs/solutions/ for future agent sessions.
argument-hint: optional path to a specific spec file
---

Use the Agent tool with `subagent_type: "compound"` to handle this request. Pass the user's full request to the agent as its prompt.

User request: $ARGUMENTS
